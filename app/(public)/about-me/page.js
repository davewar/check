import React from "react";
import About from "./About";

export const metadata = {
  title: "About Me",
};

const Page = () => {
  return (
    <main>
      <About />
    </main>
  );
};

export default Page;
